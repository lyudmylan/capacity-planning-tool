# Shipping Workflow

This file is a short entry point for the shipping process.

The canonical workflow now lives in [`../AGENTS.md`](../AGENTS.md).

Use `AGENTS.md` as the source of truth for:

- default implementation sequence
- delegation guidance
- merge standard
- local verification commands

Keep this file short on purpose so workflow rules do not drift across two Markdown files.

Clean up the repo documentation set.

Goals:
- reduce duplication
- make sources of truth explicit
- keep always-loaded docs minimal
- separate repo-specific rules from reusable shared workflow guidance
- preserve clear entry points for humans and agents

Review first, then propose or implement a simpler doc structure.
